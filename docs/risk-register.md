# Shooting Brake Risk Register

## Purpose and status

This register applies to the production direction in [`../plan.md`](../plan.md): upstream vLLM 0.26+ on one RTX 5090 as the CUDA state owner, plus one isolated persistent QuixiCore-XPU B70 routed-expert provider connected through a versioned pinned-memory request ring and a Qwen-scoped out-of-tree post-top-k adapter.

Colibri has proven important transport, placement, numerical, and failure-recovery mechanics. Those results reduce feasibility uncertainty but do not clear the production risks below. Unless a row explicitly says otherwise, every risk remains **open** until its Phase 0–10 gate is recorded against the production implementation.

Risk responses follow three rules:

1. Correctness failures are never accepted as performance tradeoffs.
2. A model/provider combination that fails capability negotiation remains on stock vLLM CUDA; it is never sent through a generic or guessed B70 path.
3. CPU matrix execution is exact emergency recovery only. If it becomes normal-path work, the production design has failed its operating contract.

## Production integration risks

| ID | Risk and trigger | Evidence required | Mitigation / decision | Acceptance gate |
|---|---|---|---|---|
| R1 — upstream seam drift | vLLM changes `MoERunner`, `RoutedExperts`, router, modular-kernel, graph, or expert-map APIs, or the adapter requires a broad fork. | Exact vLLM commit, adapter compatibility tests, and all-CUDA execution through the adapter. | Keep `HybridMoERunner` / `HybridRoutedExperts` out of tree and Qwen-scoped; absorb upgrades only in the narrow adapter. Unsupported configurations use stock vLLM CUDA. | Phase 4 all-CUDA adapter output and performance match stock vLLM within the declared tolerance/noise. |
| R2 — old patched host adoption | The llm-scaler vLLM 0.21.0 patch is applied to the 0.26+ CUDA host or becomes a second serving runtime. | Dependency and patch inventory for both processes. | Use upstream vLLM 0.26+ as the only CUDA host. Treat llm-scaler as a secondary kernel reference and INT4 fallback, never as the primary provider or host patch. | Startup provenance identifies independent pinned CUDA-host and provider versions; no old host patch is present. |
| R3 — provider dependency drift | A QuixiCore-XPU, oneAPI, Level Zero, Intel driver, or selected binding/runtime version change—or an llm-scaler or `vllm-xpu-kernels` change on the secondary INT4 path—alters operator signatures, supported shapes, safety conditions, layouts, activation behavior, or numerics. | Pinned provider image/environment, dependency fingerprint, capability response, direct XPU matrix, and mixed-shape sequential runs. | Qualify each primary or secondary provider bundle independently; preserve or prove unnecessary historical safety guards; fail startup on unsupported capabilities. | Phase 0 manifest/handshake and Phase 1 direct-provider gates pass for the pinned bundle. |
| R4 — incompatible weight artifacts | CUDA and B70 weights represent different source checkpoints, or NVFP4 E2M1 packing, block-16 E4M3 scales, per-expert FP32 global scales, gate/up ordering, padding, or Colibri-only GS64 semantics are confused. | Higher-precision source identity; CUDA/B70 artifact fingerprints; one-expert projection comparisons; supported format manifest. | Generate both artifacts from the identical source model; convert only offline or at initialization; never reinterpret Colibri GS64 or a secondary INT4 artifact as production NVFP4 or convert on the token path. | Phase 3 provider and mixed-result tolerances pass for every declared format and shape. |
| R5 — canonical route violation | B70 recomputes router logits, softmax, top-k, shared-expert work, or changes IDs/weights while partitioning routes. | Captured vLLM `topk_ids`/`topk_weights`, local/remote masks, token-route map, and per-layer result comparison. | The CUDA state owner remains authoritative. B70 accepts only canonical preselected IDs and weights and returns only its weighted routed partial. | Masks partition every selected route exactly once; Phase 3 and Phase 6 comparisons pass. |
| R6 — local CUDA route incompatibility | The chosen CUDA MoE backend cannot skip arbitrary remote routes or interprets `-1` mappings unsafely. | Backend-specific mixed local/remote cases, including no-local and no-remote batches. | Explicitly qualify skip semantics; otherwise compact local routes and unpermute/reduce in `HybridRoutedExperts`. Never rely on undocumented behavior. | Phase 6 per-layer routed output matches the all-CUDA reference. |
| R7 — stale or torn ring transactions | Ring wraparound, weak publication ordering, slot reuse, placement reload, or provider restart pairs a completion with the wrong request or weights. | Stress tests for concurrent slots, wraparound, sequence mismatch, invalid generation, restart, timeout, and late completion. | Version the protocol; carry request sequence, slot, layer, placement generation, weight generation, status, and negotiated bounds; use explicit acquire/release publication. | Phase 2 accepts no stale output and loses no selected contribution under every injected case. |
| R8 — process/runtime interference | Loading CUDA, QuixiCore-XPU, oneAPI, Level Zero, and any selected provider binding or secondary PyTorch-XPU dispatch stack together corrupts runtime state or makes failures non-isolatable. | Process topology, device selection, health checks, crash/restart injection, and resource accounting. | Run one persistent isolated B70 provider process. It owns only B70 weights, fixed XPU buffers, streams, kernel selection, and request execution. | Phase 1 explicit B70 selection and Phase 9 loss/restart qualification pass. |
| R9 — batching gap | The single-token native reference is mistaken for a continuous-batch provider; per-request submissions, allocations, or weight uploads occur in steady state. | Direct-provider cases at `M=1`, `M=2..32`, representative larger decode and prefill `M`; allocation/upload traces; scheduler-step submission counts. | Preallocate grow-only buffers and aggregate one B70 operation per active layer and scheduler step. Select tiny/grouped/prefill kernels by qualified capability thresholds. | Phase 1 and Phase 7 gates pass with no steady-state weight upload or tensor allocation. |
| R10 — incomplete weighted partial | Duplicate, unsorted, missing, or near-zero routes; compact remapping; or mixed local/remote batches produce the wrong `[M_remote, hidden]` wire partial or full `[M, hidden]` CUDA scatter target. | Independent provider oracle for all staged-route patterns listed in Phase 3, including an already-staged row whose valid remote subset becomes empty and repeated experts across tokens; adapter/ring coverage proves that a full zero-remote batch issues no provider request. | Return a weighted, reduced compact partial in staged-row order with exact token/route mapping and explicit status; CUDA scatters it into a zero-initialized full-batch buffer. | Phase 3 passes every supported staged shape; unsupported shapes fail explicitly. |
| R11 — exposed synchronization | `.item()`, Python polling, device-wide synchronization, pageable buffers, or serialized issue/take dominates the vLLM critical path. | CUDA/XPU/host timeline separating publication, copies, queue, kernel, completion, H2D join, and exposed wait. | Use fixed pinned buffers, dedicated copy streams/events, asynchronous publication and join, and no per-forward allocations. Optimize only after eager correctness. | Phase 8 shows no steady-state device-wide synchronization and preserves or improves eager throughput; Phase 10 confirms bounded exposed wait under the production workload. |
| R12 — CUDA graph incompatibility | Full graph capture incorrectly assumes external Level Zero work is a CUDA operation, causing hangs, stale data, or hidden eager fallback. | Eager and graph-mode traces with explicit graph breaks and external-completion events. | Start eager; restore PIECEWISE CUDA graphs with a break around the provider. ExLlama stream-memory mechanics are an optimization reference, not proof that XPU work is graph-captured. | Phase 8 preserves output agreement and improves or maintains eager throughput. |
| R13 — silent route loss on failure | Timeout, device loss, kernel error, cancellation, or provider-generation mismatch drops a B70 contribution or accepts a late reply. | Injected failures at issue, execution, copy, join, restart, and cancellation boundaries; exact selected-route accounting. | Mark exact failed token-route entries; recompute on an available CUDA/CPU correctness path or fail explicitly. Reject late and wrong-generation completions. | Phase 9 demonstrates no silently incomplete output. |
| R14 — recovery becomes normal compute | CPU or recovery CUDA executes routinely because deadlines, queueing, placement, or provider health are poor. | Normal/recovery route counts, reason codes, queue and deadline distributions under representative load. | Keep CPU orchestration-only in normal operation; bound queues and backpressure; narrow or disable B70 participation if exact recovery is no longer exceptional. | Phase 10 records zero normal-path CPU matrix work and an agreed exceptional recovery rate. |
| R15 — weight movement enters foreground | Expert weights are uploaded, evicted, converted, or read from storage during decode/prefill requests. | Per-request CUDA/B70/host/storage weight-transfer traces and loaded-generation telemetry. | Load immutable compact CUDA/B70 ownership before serving; keep expert artifacts resident for the placement generation. | Phase 5 proves exactly one normal owner per expert and no inference-time weight movement. |
| R16 — invalid capacity or ownership | The placement map has holes, overlaps, stale compact slots, or moves hot traffic to B70 solely to equalize route share. | Manifest-checked ownership, capacity bytes, traffic traces, placement fingerprint, and replayed cost model. | Optimize for capacity and critical-path cost, not equal traffic. CUDA keeps the highest-traffic experts; B70 supplies cold expert capacity. | Phase 5 coverage is exact and Phase 10 reports capacity gained with throughput/latency cost. |
| R17 — provider overhead dominates | QuixiCore-XPU dispatch, tensor copies, or provider bookkeeping dominates qualified NVFP4 MoE kernel time. | Provider-side profile across decode and prefill batch classes, compared with the native Colibri B70 worker where shape-compatible. | Preserve the architecture; use QuixiCore-XPU's framework-neutral C++ ABI if profiling identifies material binding overhead. Do not preemptively fork kernels. | The selected provider meets the measured overhead target without changing canonical math or protocol semantics. |
| R18 — production performance inferred from Colibri | Single-token Colibri timings or like-for-like Colibri throughput are presented as production vLLM results. | Same-checkpoint/source, same-workload Phase 10 comparison against stock all-CUDA vLLM and defined offload controls. | Label Colibri numbers as reference evidence only. Measure batching, graph boundaries, overlap, queueing, TTFT, ITL, throughput, capacity, memory, and correctness in production. | Only Phase 10 can support a production performance claim. |
| R19 — unsupported model silently enabled | A newly supported upstream Qwen or other MoE model enters B70 execution based on its name or approximate shape. | Explicit model, weight-artifact, provider, and placement manifests plus startup handshake. | Facts, not model-name conditionals, select the provider. Unsupported models remain unchanged on stock vLLM CUDA. | Phase 0 startup fails closed on every architecture, shape, dtype, top-k, layout, group-size, or generation mismatch. |

## Evidence already available, and its limit

The Colibri Qwen3.6 implementation provides reference evidence for:

- persistent B70 expert residency and compact layer/expert ownership;
- host-staged activation/metadata/weighted-partial transport;
- asynchronous single-token issue/take;
- Colibri-only signed-S4 GS64 conversion and ESIMD execution;
- numerical agreement with the Colibri CPU reference;
- exact failed-route recomputation;
- end-to-end CUDA+B70 generation with zero normal-path CPU expert fallback in the recorded suite.

Phase 1 adds direct evidence for explicit B70 selection, exact full-bank startup validation, fixed provider buffers, split/fused batched execution through `M=128`, duplicate top-8 weighting, generation/sequence rejection, allocation stability, and shutdown. Phase 2 adds a separately mapped CUDA/Level-Zero pinned-memory probe, the protocol-v2 eight-slot process ring, deterministic lifecycle/failure stress through 2,000,000-request wraparound, process-isolated real-B70 numerical execution, exact dispatch/allocation accounting, and warmed stage percentiles. Together they close the direct-provider and direct-process-ring portions of R3, R7, R8, R9, and R17 for the pinned bundle. They do **not** close production placement, the full provider mathematics matrix, upstream-vLLM integration, scheduler aggregation/overlap, operational recovery, or production-performance risks; those belong to Phases 3–10.

## Stop and fallback rules

1. Any selected-route correctness mismatch, stale completion, ownership overlap/hole, or silently incomplete output stops hybrid qualification.
2. Any format or capability mismatch fails at startup; it is not repaired with an undocumented generic kernel.
3. A provider failure either triggers exact route recovery or fails the request explicitly.
4. If B70 cannot provide capacity or performance benefit after full production accounting, the affected model/configuration remains on stock all-CUDA vLLM; Colibri reference success is not grounds to force deployment.
5. If provider-wrapper overhead alone is material, the framework-neutral QuixiCore-XPU C++ ABI may replace the wrapper behind the same protocol. This does not justify changing the CUDA host, router semantics, or failure contract.
6. Thresholds for numerical tolerance, timeout, recovery rate, graph noise, and performance acceptance must be declared before the corresponding gate is run.
